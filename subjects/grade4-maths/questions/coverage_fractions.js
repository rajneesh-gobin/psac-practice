'use strict';
(function(){
const num=(id,s,q,a,h,e)=>STATIC_QUESTIONS.push(makeNum({id,chapterId:'g4-fractions',subsection:s,difficulty:2,question:q,answer:String(a),acceptableAnswers:[String(a)],hint:h,explanation:e||`The answer is <b>${a}</b>.`}));
const mcq=(id,s,q,o,a,h,e)=>STATIC_QUESTIONS.push(makeMCQ({id,chapterId:'g4-fractions',subsection:s,difficulty:2,question:q,options:o,answer:a,hint:h,explanation:e||`<b>${a}</b> is correct.`}));
for(let i=0;i<13;i++){const d=5+i,a=i+1,b=i+2;mcq(`g4m-cov-frcmp-${i}`,'compare_order',`Which fraction is greater: ${a}/${d} or ${b}/${d}?`,[`${a}/${d}`,`${b}/${d}`],`${b}/${d}`,'With the same denominator, compare the numerators.',`Both fractions are cut into the same number of parts (${d}), so the one with more parts is bigger. ${b} is more than ${a}, so <b>${b}/${d}</b> is greater.`);}
for(let i=0;i<13;i++){const a=i+2,b=i+3,k=2+i%3,correct=`${a*k}/${b*k}`;mcq(`g4m-cov-freq-${i}`,'equivalent',`Which fraction is equivalent to ${a}/${b}?`,[correct,`${a}/${b*k}`,`${a*k}/${b*k+1}`,`${a+1}/${b}`],correct,'Multiply the numerator and denominator by the same number.',`Multiply the top AND the bottom by the same number, ${k}: ${a} \u00d7 ${k} = ${a*k} and ${b} \u00d7 ${k} = ${b*k}. That gives <b>${correct}</b>, which is the same amount as ${a}/${b}.`);}
for(let i=0;i<16;i++){const d=6+i,a=i%4+1,b=i%3+1;num(`g4m-cov-fradd-${i}`,'add_sub',`Calculate: ${a}/${d} + ${b}/${d}. (Write as A/B)`,`${a+b}/${d}`,'The denominators are the same: add the numerators.',`The parts are the same size, so count how many there are: ${a} + ${b} = ${a+b}. The bottom number stays ${d}, giving <b>${a+b}/${d}</b>.`);}
for(let i=0;i<16;i++){const d=2+i%5,n=1+i%(d-1),k=3+i;num(`g4m-cov-frof-${i}`,'fraction_of',`What is ${n}/${d} of ${d*k}?`,n*k,'Divide by the denominator, then multiply by the numerator.',`Split ${d*k} into ${d} equal parts: ${d*k} \u00f7 ${d} = ${k}. Then take ${n} of those parts: ${k} \u00d7 ${n} = <b>${n*k}</b>.`);}
for(let i=0;i<19;i++){const d=3+i%5,w=1+i%4,n=1+i%(d-1),imp=w*d+n;num(`g4m-cov-frmix-${i}`,'mixed_numbers',`Write ${w} ${n}/${d} as an improper fraction. (Write as A/B)`,`${imp}/${d}`,'Multiply the whole number by the denominator, then add the numerator.',`Each whole is ${d} parts, so ${w} whole${w===1?'':'s'} is ${w} \u00d7 ${d} = ${w*d} parts. Add the ${n} spare part${n===1?'':'s'}: ${w*d} + ${n} = ${imp}. That is <b>${imp}/${d}</b>.`);}
for(let i=0;i<19;i++){const d=i<9?10:100,n=i<9?i+1:(i-8)*11;num(`g4m-cov-frdec-${i}`,'decimals',`Write ${n}/${d} as a decimal.`,n/d,'A denominator of 10 means tenths; 100 means hundredths.',`A bottom number of ${d} means ${d===10?'tenths, so the digits go one place after the point':'hundredths, so the digits go two places after the point'}: ${n}/${d} = <b>${n/d}</b>.`);}
const contexts=['A class read','A gardener watered','A baker used','A runner completed','A painter coloured','A child drank','A recipe needs','A ribbon was cut into','A farmer sold','A team won','A library lent','A bus travelled','A tank was filled'];
contexts.forEach((c,i)=>{const d=[4,5,6,8][i%4],n=1+i%(d-1),total=d*(5+i);num(`g4m-cov-frwp-${i}`,'word_problems',`${c} ${n}/${d} of ${total} pages/items. How many pages/items is that?`,n*(5+i),'First find one equal part by dividing by the denominator.',`One part first: ${total} \u00f7 ${d} = ${5+i}. Then take ${n} part${n===1?'':'s'}: ${5+i} \u00d7 ${n} = <b>${n*(5+i)}</b>.`);});
})();
